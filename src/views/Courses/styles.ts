import { createStyles } from '@material-ui/core/styles';

const styles = createStyles( {
    paperClass: {
        display: 'flex',
        alignItems: 'center'
    },
    progressClass: {
        marginLeft: '2em'
    }
} );

export default styles;
