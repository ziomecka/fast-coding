import { createStyles } from '@material-ui/core/styles';

const styles = createStyles({
    lessonsPaper: {
        display: 'flex',
        alignItems: 'center'
    },
    lessonsProgress: {
        marginLeft: '2em'
    }
});

export default styles;