import * as React from 'react';
import NewPassword from '../../app/NewPasswordForm/';

const NewPasswordView: React.StatelessComponent<{}> = () => {
    return <NewPassword />;
};

export default NewPasswordView;
