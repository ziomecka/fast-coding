import * as React from 'react';
import NewPassword from '@forms/NewPasswordForm/';

const NewPasswordView: React.StatelessComponent<{}> = () => {
    return <NewPassword />;
};

export default NewPasswordView;
